# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.md

# Cargo / Tauri build output
target/

# Stale Bun binary lockfile (project uses bun.lock as of Bun 1.2+)
bun.lockb

# Git worktrees
.worktrees/

# Claude Code project-local state
.claude/

# Playwright MCP artifacts
.playwright-mcp/

# Playwright E2E artifacts
test-results/
playwright-report/
.playwright/

# Auto-generated by Acorn on every control-session PTY spawn. Drops the
# acorn-ipc primer into the worktree as a marker for agents that read
# project-local docs but don't have an inline-system-prompt flag we hook.
.acorn-control.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
